box: jparyani/docker@1.0.1
build:
  steps:

    - name: 'Build docker image'
      code: |
        $(docker build . -t myapp)

    - name: 'Run tests'
      code: |
        docker run myapp bundle --without "" && rake test


