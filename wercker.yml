box: wercker-labs/docker
build:
  steps:
    - script:
        name: 'Build docker image'
        code: |
          docker build -t myapp .

    - script:
        name: 'Run tests'
        code: |
          docker run myapp bundle --without "" && bundle exec rake test
